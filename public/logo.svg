<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 72 40" fill="none">
  <!--
    Minimalist wordmark, 72x40 canvas.

    Mark = two interlocking rings (chain-link / Olympic illusion).
    Intersections at (14, 13.07) and (14, 26.93). Render order:
      1. purple ring (full)
      2. teal ring (full) — currently covers purple at BOTH crossings
      3. short purple arc over TOP crossing only

    Result: top crossing shows purple on top, bottom crossing shows
    teal on top → rings appear woven / linked.

    Gradients are userSpaceOnUse so the overlay arc blends seamlessly
    with the underlying ring 1 (shared color stops in absolute coords).
  -->

  <defs>
    <linearGradient id="lg-purple" x1="0" y1="12" x2="0" y2="28" gradientUnits="userSpaceOnUse">
      <stop offset="0%"  stop-color="#c4b5fd"/>
      <stop offset="100%" stop-color="#7c3aed"/>
    </linearGradient>
    <linearGradient id="lg-teal" x1="0" y1="12" x2="0" y2="28" gradientUnits="userSpaceOnUse">
      <stop offset="0%"  stop-color="#5eead4"/>
      <stop offset="100%" stop-color="#0d9488"/>
    </linearGradient>
    <filter id="text-shadow" x="-10%" y="-10%" width="120%" height="140%">
      <feDropShadow dx="0" dy="0.5" stdDeviation="0.4" flood-color="#000000" flood-opacity="0.55"/>
    </filter>
  </defs>

  <!-- Interlocking rings — thin delicate stroke (1.75 on r=8) matching the
       original sleek aesthetic. Round linecaps on every stroke end so the
       weave overlay blends seamlessly with the ring it sits on. -->
  <g fill="none" stroke-width="1.75" stroke-linecap="round">
    <circle cx="10" cy="20" r="8" stroke="url(#lg-purple)"/>
    <circle cx="18" cy="20" r="8" stroke="url(#lg-teal)"/>
    <!-- Weave overlay: 30° arc centered on top intersection (θ=-75° to -45°). -->
    <path d="M 12.07 12.27 A 8 8 0 0 1 15.66 14.34" stroke="url(#lg-purple)"/>
  </g>

  <!-- Wordmark -->
  <g font-family="ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif" fill="#ffffff" filter="url(#text-shadow)">
    <text x="31" y="19" font-size="10.5" font-weight="400" letter-spacing="0.35">Collab</text>
    <text x="31" y="32" font-size="10.5" font-weight="600" letter-spacing="-0.2">Planner</text>
  </g>
</svg>
